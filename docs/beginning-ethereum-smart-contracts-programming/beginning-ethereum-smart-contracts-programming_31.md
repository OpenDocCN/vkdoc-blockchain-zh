# 索引

## A

- `add_node()` 成员
- `add_transaction()` 方法
- `admin.addPeer()` 函数
- `admin.peers` 属性
- 算法稳定币
- `announceWinners()` 函数
- 反洗钱 (AML)
- `append_block()` 方法
- 应用二进制接口 (ABI)
- 专用集成电路 (ASIC)
- `--authrpc.port` 选项
- 自动编译选项
- 雪崩效应

## B

- `base64` 编码的 JSON 字符串
- `base64` 编码的字符串
- `base64` 编码
- 棒球卡
- `bet()` 函数
- 币安美元 (BUSD)
- BIP 39 (比特币改进提案) 种子短语
- 比特币
- 比特币的实现
- 32 位指令
- 区块链
- 区块
- 区块头
- 区块
- 确认数
- 中心化数据库与机构
- 链接组件
- 共识协议
- 去中心化数据库
- 分布式账本
- 创世区块
- 不可篡改性
- 默克尔根
- 默克尔树
- 权益证明
- 工作量证明
- 测试
- 交易
- 时间戳
- 节点类型
- `Blockchain` 类
- 区块链网络
- `blockchain.py`
- Brew
- 广播
- `buildTransaction()` 函数
- 字节码
- `bytes()` 函数

## C

- 凯撒密码
- 计算器
- `calculator.sol`
- `calldata` 关键字
- `call()` 函数
- `cashOut()` 函数
- 中心化交易所 (CEX)
- `checkEduCredentials` 按钮
- `checkEduCredentials()` 函数
- 谷歌 Chrome 网上应用店
- 密文
- `compareStrings()` 函数
- 概念性区块链实现，Python
  - 添加交易
  - 追加区块
  - 通过 REST API 暴露 `Blockchain` 类
  - 类声明代码
  - 寻找随机数
  - 导入模块和库
  - 安装 Flask
  - 获取完整区块链
  - 获取随机数
  - 执行挖矿
- 共识协议
- `contract` 关键字
- 加密货币初学者
- 加密货币
- 加密货币交易所
  - 中心化交易所
  - 去中心化交易所
  - 地址
  - `approve()` 函数
  - 创建细节
  - `DEX.sol` 事件
  - 兑换代币
  - LWM 代币
  - Remix IDE
  - 代币余额
  - WML 代币
  - 兑换
  - 授权按钮
  - 兑换
  - `exchange()` 函数
  - LWM 代币权限
  - 代币余额
  - WML 代币
  - 代币合约创建与部署
  - 类型
- 加密算法
- 加密哈希
- 密码学模块
- 密码学
  - 非对称
  - 见"非对称密码学"
  - 凯撒密码
  - 密文定义
  - 哈希
  - 对称密码学
  - 类型
- 区块链中的密码学
  - 加密算法
  - 数字签名
  - 哈希
  - 对称与非对称密码学
- 货币

## D

- DAI 储蓄利率 (DSR)
- 数据加密
- 去中心化
- 去中心化自治组织 (DAO)
- 去中心化交易所 (DEX)
- 去中心化金融 (DeFi)
- `decrypt()` 函数
- 开发者
- `difficulty_target`
- 数字签名
- 分布式账本

## E

- 教育证书
- `eduCredentialsStore` 合约
- `eduCredentialsStore.functions.storeEduCredentials()` 函数
- `eduCredentialsStore.sol`
- `eduCredentialsStore` 变量
- 椭圆曲线数字签名算法 (ECDSA)
- `encrypt()` 函数
- 节能共识协议
- 节点公钥 (Enode)
- 节点公钥值
- `.env` 文件
- `eth.blockNumber` 属性
- `eth.coinbase` 属性
- `eth.contract()` 函数
- 以太坊
- 以太坊区块链
- 以太坊客户端
  - 定义
  - 在 Linux 上安装 Geth
  - 在 macOS 上安装 Geth
  - 在 Windows 上安装 Geth
  - 类型
- 以太坊加密钱包 MetaMask
  - 见 MetaMask
- 以太坊网络
- 以太坊协议
- 以太坊意见征求 (ERC)
- 以太坊测试网络
- 以太坊交易
- 以太坊虚拟机 (EVM)
- Ether 单位
- Etherscan
- `eth.gas_price` 属性
- `eth.getBalance()` 函数
- `eth.getBlock()` 函数
- `eth.getBlock(eth.blockNumber).miner`
- `eth.get_transaction_count()` 函数
- `eth.send_raw_transaction()` 函数
- `eth.sendTransaction()` 函数
- EVM 兼容性
- `exchange()` 函数

## F

- `Fernet` 类
- 芬尼
- Flask 微框架
- 全节点
- 可互换的

## G

- Gas
- Gas 费用
- `generate_key()` 函数
- 创世区块
- `getCurrentAccount()` 函数
- Geth 目录
- Geth 客户端
- Geth JavaScript 控制台
- Geth 源码
- 格尔利测试网
- 格尔利测试网络
- GPU 擅长

## H

- `hash_block()` 方法
- 哈希函数
  - 区块链数据映射定义
  - 特性
  - 固定长度输出
  - `hashlib` 模块
  - 不可篡改
  - 多台计算机
  - 下一个区块的角色
  - `sha256()` 函数
  - 交易
- 哈希
- 前一个区块的哈希
- 交易 A 的哈希 (H[A])
- 交易 B 的哈希 (H[B])
- HTTP
- 恶性通货膨胀

## I

- 相同区块链
- 索引
- `index.html`
- Infura
- INFURA 节点
- `__init__()` 函数
- IPC
- IPFS

## J

- JSON 字符串
- Jupyter Notebook

## K

- Keccak256 哈希算法
- Keccak256 哈希
- `keystore` 目录
- `kill()` 函数
- 了解你的客户 (KYC)

## L

- `last_block`
- 许可证
- 轻节点
- 清算流程
- `loadContract()` 函数
- `load()` 函数
- `loadWeb3()` 函数

## M

- `main.css`
- 以太坊主网
- 管理账户
  - coinbase
  - 设置私钥
  - 公钥
  - 删除账户
  - UTC 文件
  - 映射对象
  - `memory` 关键字
- 默克尔根
- 默克尔树
- MetaMask
  - 创建额外账户
  - 创建新钱包
  - 输入密码
  - 以太坊加密钱包
  - 导出账户
  - Chrome 上的扩展图标
  - 导入账户
  - INFURA 节点
  - 安装 MetaMask 扩展
  - 固定到 Chrome 浏览器
  - 恢复账户
  - 角色
  - 秘密恢复短语
  - 切换账户
  - 转账 Ether
  - 查看钱包
- MetaMask Chrome 扩展
- MetaMask 扩展
- 矿工哈希
- `miner.setEtherbase()` 函数
- 矿工加入
- `miner.start()` 函数
- `miner.stop()` 函数
- 矿工/验证者
- 挖矿节点
- 挖矿过程
- MIT 许可证
- MyTestNet
- MyTestNet 目录
- MyTestNet 文件夹
- `~/MyTestNet/data/node1/keystore` 目录

## N

- 网络广播
- 网络难度级别
- 网络难度目标
- `--networkid` 选项
- 网络延迟
- `node2` 目录
- Node.js 项目
- 节点
- 节点的区块链
- `--nodiscover` 选项
- `nodes` 成员
- 随机数
- 非同质化代币 (NFT)
  - 买家
  - 买卖
  - 部署
  - ERC-721
  - OpenSea
  - 所有权 *对比* 版权
  - 测试
  - 账户余额，地址
  - 数字资产
  - 铸造
  - 名称/符号
  - 所有者
  - 所有权
  - TokenURI
  - 交易
  - 代币合约
- 一次性使用的数字

## O

- 在线彩票游戏
  - 投注玩家
  - 计算
  - 提现功能
  - 完整合约
  - 支付玩家
  - 智能合约 Betting 声明
  - 关键点
  - 映射对象
  - `playerAddressesArray`
  - `playerDetailsMapping`
  - Solidity 结构体
  - 状态/中奖号码
  - Web 前端
  - 见 Web 前端
  - 中奖号码/中奖者
  - `announceWinners()` 函数
  - 创建数组
  - 所有者
  - `playerAddressesArray`
  - `transfer()` 函数
  - 变量，重置
- OpenZeppelin

## P, Q

- `package.json`
- Parity
- 当铺
- 点对点模式
- `personal.newAccount()` 函数
- `personal.unlockAccount()` 函数
- `--port` 选项
- Pragma Solidity 语句
- `private` 关键字
- 私有以太坊测试网络
  - 创建
  - 区块链节点初始化
  - 创建文件夹，存储节点数据
  - 创世区块
  - 节点
  - 节点启动
  - 见"启动节点"
  - 对等节点与交易
- 私有函数
- 私钥
- 编程语言
- `proofFor()`
- 权益证明 (PoS)
  - 以太坊 *对比* 工作量证明
  - 交易费用
  - 交易
  - 验证者
- `proof_of_work()` 函数
- 证明
- 公有区块链
- 公钥
- `pure` 关键字
- 基于 Python 的 dapp
- `python-dotenv` 模块
- Python 程序

## R

- Remix IDE
- Remix VM
- `require()` 函数
- 结果哈希
- 根密钥
- Ropsten 测试网络

## S

- 秘密恢复短语
- `selfdestruct()` 函数
- `send()` 函数
- Sepolia 测试网络
- `sha256()` 函数
- SHA256 哈希函数
- 共享密钥
- `sign_transaction()` 函数
- 简化支付验证 (SPV)
- 智能合约
  - ABI
  - 字节码
  - 编译合约
  - 创建定义
  - 部署
  - 教育证书
  - Ether
  - 函数
  - 资金
  - Ganache Provider
  - Injected Provider-Metamask
  - 与用户交互
  - Remix IDE
  - 编译合约
  - 创建新合约
  - Remix VM
  - `selfdestruct()` 函数
  - 状态变量作为证明存储
  - 测试第一个函数
  - 测试第二个函数
  - 区块链的使用
  - 使用 Goerli 测试网
  - Visual Studio Code
- 稳定币
  - 比特币
  - 加密资产抵押
  - DAI
  - DAO
  - DAI 购买
  - 抵押率
  - DAO 清算价格
  - 智能合约的使用
  - 定义
  - 法币抵押
  - 非抵押
  - 类型
- `start()` 函数
- 启动节点
  - 账户余额检查
  - 账户创建
  - 区块检查
  - 获取信息
  - 挖矿
  - 配对
  - 执行挖矿
  - 启动另一个节点
  - 停止节点
  - 转账 Ether
- 状态变量
- `storeEduCredentials()` 函数
- `storeEduCredentials` 按钮
- `storeProof()`
- 对称密码学
  - 方法
  - 使用私钥解密
  - 定义
  - 使用公钥加密
  - 公钥算法
  - 公钥/私钥对生成
  - 共享密钥
  - 共享密钥生成
  - 对称解密
  - 对称加密
  - 用途与应用
  - 验证数字签名
- 同步区块链
  - `blockchain.py` 应用
  - 多个节点
  - `/nodes/add_nodes`
  - `nodes` 成员
  - `update_blockchain()` 方法
  - `valid_chain()`
- Szabo

## T

- 测试，合约 ABI
  - 账户
  - 投注号码
  - 投注状态
  - 部署函数
  - `getGameStatus`
  - Remix IDE
  - 赢家公告
  - 余额计算
  - 事件
  - 中奖号码
- `TestWeb3.html`
- 时间戳
- 代币合约
  - 创建
  - `decimals()` 函数
  - 部署参数
  - 买家账户
  - Ether
  - 隐藏 WML
  - MetaMask 交易，Etherscan
  - Ether
  - MetaMask 修改
  - NFT
  - 见"非同质化代币 (NFT)"
  - 出售
  - 买家
  - `require()` 函数
  - 代币所有者
  - 代币价格
  - 代币
  - `_transfer()` 函数
  - `transfer()` 事件
  - `unitsOneEthCanBuy`
  - 出售代币的智能合约支付
  - `approve()` 函数
  - 余额减少
  - `checkEduCredentials()` 函数
  - `eduCredentialsStore`
  - `EduCredentialsStore.sol`
  - Etherscan
  - `getBalance`
  - MetaMask 权限
  - 重新部署
  - `token.sol`
  - 代币单位的实用性
- 代币
  - 销毁
  - 嘉年华游戏
  - 嘉年华所有者 *对比* 代币
  - ERC-20 定义
  - 函数/事件接口
  - 以太坊区块链
  - Ether 的实现
  - 铸造新代币
  - 平台单位
- 传统金融
  - 活动
  - 问题/局限性
- 交易费用
- 交易
- True USD (TUSD)
- 信任
  - 去中心化
  - 商品/服务
  - 机构
  - 问题定位
  - 12 个单词的密码短语
  - 12 个单词的秘密短语

## U

- 未确认交易
- `update_blockchain()` 方法
- USD Coin (USDC)
- USD Tether (USDT)

## V

- 验证者
- `valid_chain()` 方法
- `valid_proof()` 方法
- `verify()` 函数
- `view` 关键字
- Visual Studio Code

## W, X, Y, Z

- `web3-bzz`
- Web3 去中心化应用 (dapp)
  - 使用 Python 创建 dapp
  - `base64` 编码
  - 加载教育证书
  - 合约验证结果
  - 使用 Python 与以太坊交互
  - 检查账户 1 的余额
  - 连接到 Infura
  - 获取一个区块
  - 在 Infura 注册
  - 设置以太坊账户
  - 在账户之间转账 Ether
- `web3-eth`
- `web3.fromWei()` 函数
- `web3.js`
  - 定义
  - 安装 web3.js
  - 与智能合约交互
  - 模块
  - 使用 MetaMask
- `web3projects` 文件夹
- `web3projects` 文件夹
- `web3.py` 库
- `web3-shh`
- `web3-utils`
- Web 前端
  - 账户
  - 提现
  - Chrome 实例
  - 消息
  - `OnlineBetting.html` 文件
  - 测试
  - 中奖号码
- WebSocket
- Wei
- `window.ethereum`
